// New World Cargo style reminder: Poppins, light command-center canvas, Cargo Yellow action color, lavender route accents, mobile-first.

import { ArrowLeft, CheckCircle2, CircleHelp, Download, FileCheck2, MoreHorizontal, PackageOpen, PhoneCall, RotateCcw, Share2 } from "lucide-react";
import { useState } from "react";
import { useLocation, useRoute } from "wouter";
import { toast } from "sonner";
import { ASSETS, shipments } from "@/lib/mock-data";
import { ShipmentActions, StatusBadge, Timeline } from "@/components/shipment-ui";

export default function ShipmentDetail() {
  const [, params] = useRoute("/shipments/:id");
  const [, navigate] = useLocation();
  const shipment = shipments.find((s) => s.id === params?.id) ?? shipments[0];
  const [showReschedule, setShowReschedule] = useState(false);
  const [showDetails, setShowDetails] = useState(false);
  return <div className="mx-auto max-w-5xl"><button onClick={() => navigate("/shipments")} className="mb-6 flex items-center gap-2 text-xs font-bold text-white/45 transition hover:text-white"><ArrowLeft className="size-4" />Back to shipments</button><div className="grid gap-6 lg:grid-cols-[1fr_0.78fr] lg:items-start"><div><div className="relative overflow-hidden rounded-[32px] bg-cargo-yellow p-6 text-ink sm:p-8"><div className="absolute inset-0 opacity-20" style={{ backgroundImage: `url(${ASSETS.package})`, backgroundPosition: "right center", backgroundSize: "cover", mixBlendMode: "multiply" }} /><div className="relative"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.2em] text-ink/45">{shipment.category}</p><h1 className="mt-2 font-heading text-3xl font-extrabold tracking-tight">{shipment.trackingNumber}</h1></div><button onClick={() => toast("More shipment actions coming next.")} className="grid size-10 place-items-center rounded-full bg-ink/10"><MoreHorizontal className="size-5" /></button></div><div className="mt-8 flex items-center justify-between gap-3"><div><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-ink/45">{shipment.origin.split(",")[0]}</p><p className="mt-1 text-lg font-bold">{shipment.origin.split(",")[1]?.trim() ?? "Origin"}</p></div><div className="flex flex-1 items-center gap-1 px-2"><span className="size-2.5 rounded-full bg-ink" /><span className="h-px flex-1 bg-ink/30" /><PackageOpen className="size-5" /><span className="h-px flex-1 bg-ink/30" /><span className="size-2.5 rounded-full border-2 border-ink bg-cargo-yellow" /></div><div className="text-right"><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-ink/45">{shipment.destination.split(",")[0]}</p><p className="mt-1 text-lg font-bold">{shipment.destination.split(",")[1]?.trim() ?? "Destination"}</p></div></div><div className="mt-8 flex flex-wrap items-center justify-between gap-3 border-t border-ink/15 pt-4"><StatusBadge status={shipment.status} label={shipment.statusLabel} /><div className="text-right"><p className="text-[10px] font-bold uppercase tracking-[0.18em] text-ink/45">Estimated arrival</p><p className="mt-1 text-sm font-bold">{shipment.eta}</p></div></div></div></div><div className="mt-4"><ShipmentActions shipment={shipment} onReschedule={() => setShowReschedule(true)} /></div><div className="mt-6 rounded-[28px] border border-white/8 bg-white/[0.035] p-5 sm:p-7"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.2em] text-white/30">Where it is now</p><h2 className="mt-2 font-heading text-xl font-bold">{shipment.nextAction}</h2></div><div className="grid size-10 place-items-center rounded-2xl bg-route-lavender/15 text-route-lavender"><PackageOpen className="size-5" /></div></div><Timeline shipment={shipment} /><button onClick={() => setShowDetails(!showDetails)} className="mt-6 flex w-full items-center justify-center gap-2 rounded-2xl border border-white/10 py-3 text-xs font-bold text-white/70 transition hover:border-white/25 hover:text-white">{showDetails ? "Hide detailed journey" : "View detailed journey"}</button>{showDetails && <div className="mt-4 rounded-2xl bg-white/[0.04] p-4 text-xs leading-6 text-white/45">Hub scans and operational handoffs will appear here as New World Cargo receives live updates from the delivery network.</div>}</div></div><aside className="space-y-4 lg:sticky lg:top-24"><div className="rounded-[28px] border border-white/8 bg-white/[0.035] p-5 sm:p-6"><p className="text-xs font-bold uppercase tracking-[0.2em] text-white/30">Helpful next steps</p><div className="mt-4 space-y-2"><button onClick={() => toast.success("Delivery instructions saved for this shipment.")} className="flex w-full items-center gap-3 rounded-2xl bg-white/[0.05] p-3 text-left transition hover:bg-white/[0.1]"><CheckCircle2 className="size-5 text-mint" /><span className="text-sm font-semibold">Add delivery instructions</span></button><button onClick={() => toast("Support case form opened for this shipment.")} className="flex w-full items-center gap-3 rounded-2xl bg-white/[0.05] p-3 text-left transition hover:bg-white/[0.1]"><CircleHelp className="size-5 text-cargo-yellow" /><span className="text-sm font-semibold">Get help with this shipment</span></button><button onClick={() => toast("Courier contact will be available when assigned.")} className="flex w-full items-center gap-3 rounded-2xl bg-white/[0.05] p-3 text-left transition hover:bg-white/[0.1]"><PhoneCall className="size-5 text-route-lavender" /><span className="text-sm font-semibold">Contact support</span></button></div></div><div className="rounded-[28px] border border-white/8 bg-[#202020] p-5 sm:p-6"><p className="text-xs font-bold uppercase tracking-[0.2em] text-white/30">Shipment details</p><div className="mt-4 space-y-3 text-sm"><div className="flex justify-between gap-4"><span className="text-white/40">Package</span><span className="font-semibold">{shipment.packageName}</span></div><div className="flex justify-between gap-4"><span className="text-white/40">Service</span><span className="font-semibold">International air</span></div><div className="flex justify-between gap-4"><span className="text-white/40">Amount paid</span><span className="font-semibold">{shipment.price}</span></div></div></div><div className="grid grid-cols-2 gap-2"><button onClick={() => toast("Proof of delivery will be available after delivery.")} className="flex items-center justify-center gap-2 rounded-2xl border border-white/8 bg-white/[0.035] p-3 text-xs font-bold text-white/60"><FileCheck2 className="size-4" />Proof of delivery</button><button onClick={() => toast("Receipt download prepared.")} className="flex items-center justify-center gap-2 rounded-2xl border border-white/8 bg-white/[0.035] p-3 text-xs font-bold text-white/60"><Download className="size-4" />Receipt</button></div></aside></div>{showReschedule && <div className="fixed inset-0 z-50 flex items-end justify-center bg-black/70 p-3 backdrop-blur-sm sm:items-center"><div className="w-full max-w-md rounded-[30px] border border-white/10 bg-[#222] p-6 shadow-2xl sm:p-8"><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.2em] text-cargo-yellow">Delivery options</p><h2 className="mt-2 font-heading text-2xl font-bold">Choose a new time</h2></div><button onClick={() => setShowReschedule(false)} className="text-white/45">×</button></div><div className="mt-6 space-y-2">{["Tomorrow · 09:00–12:00", "Tomorrow · 14:00–17:00", "18 Aug · 09:00–12:00"].map((time, index) => <button key={time} onClick={() => { setShowReschedule(false); toast.success(`Delivery rescheduled for ${time}`); }} className={`flex w-full items-center justify-between rounded-2xl border p-4 text-left text-sm ${index === 0 ? "border-cargo-yellow bg-cargo-yellow/10" : "border-white/8 bg-white/[0.035]"}`}><span className="font-semibold">{time}</span><span className="size-4 rounded-full border border-white/30" /></button>)}</div><p className="mt-5 text-center text-xs text-white/35">You can change this again before the courier leaves.</p></div></div>}
  </div>;
}
