// New World Cargo style reminder: Poppins, dark command-center canvas, Cargo Yellow action color, lavender route accents, mobile-first.

import { ArrowRight, Box, Calculator, ChevronDown, MapPin, Ruler, Scale } from "lucide-react";
import { useState } from "react";
import { useLocation } from "wouter";
import { deliveryOptions } from "@/lib/mock-data";
import { CargoRail } from "@/components/shipment-ui";

export default function Quote() { const [, navigate] = useLocation(); const [from, setFrom] = useState("Guangzhou, China"); const [to, setTo] = useState("Lusaka, Zambia"); const [weight, setWeight] = useState("2"); const [size, setSize] = useState("Medium"); return <div className="mx-auto max-w-5xl"><div className="grid gap-7 lg:grid-cols-[0.9fr_1.1fr] lg:items-start"><div><p className="text-xs font-bold uppercase tracking-[0.2em] text-cargo-yellow">Price before commitment</p><h1 className="mt-2 font-heading text-4xl font-extrabold tracking-tight sm:text-5xl">Know the move.</h1><p className="mt-4 max-w-md text-sm leading-6 text-white/45">Get an estimate for a local delivery or a China–Zambia shipment in under a minute.</p><div className="mt-8"><CargoRail label="Quote route" items={["From", "Package", "To"]} active={1} /></div><div className="mt-4 flex items-center gap-2 text-xs text-white/35"><Calculator className="size-4 text-cargo-yellow" /> No account needed to start</div></div><div className="rounded-[30px] border border-white/8 bg-white/[0.035] p-5 sm:p-8"><div className="space-y-4"><QuoteField label="From" icon={MapPin} value={from} onChange={setFrom} /><div className="mx-auto flex h-3 items-center justify-center"><div className="h-8 w-px border-l border-dashed border-white/20" /></div><QuoteField label="To" icon={MapPin} value={to} onChange={setTo} /></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><QuoteField label="Weight (kg)" icon={Scale} value={weight} onChange={setWeight} type="number" /><label className="block"><span className="mb-2 block text-xs font-bold uppercase tracking-[0.14em] text-white/35">Package size</span><div className="flex items-center gap-3 rounded-2xl border border-white/10 bg-ink/35 px-4"><Ruler className="size-4 text-white/30" /><select value={size} onChange={(e) => setSize(e.target.value)} className="h-12 flex-1 appearance-none bg-transparent text-sm font-semibold text-white outline-none"><option className="bg-ink" value="Small">Small</option><option className="bg-ink" value="Medium">Medium</option><option className="bg-ink" value="Large">Large</option></select><ChevronDown className="size-4 text-white/30" /></div></label></div><div className="mt-7 space-y-3">{deliveryOptions.map((option) => <div key={option.id} className="flex items-center gap-4 rounded-2xl border border-white/8 bg-white/[0.025] p-4"><div className="grid size-10 place-items-center rounded-xl bg-cargo-yellow/12 text-cargo-yellow"><Box className="size-5" /></div><div className="min-w-0 flex-1"><p className="text-sm font-bold">{option.name}</p><p className="mt-1 text-xs text-white/38">{option.eta} · {option.detail}</p></div><p className="font-heading text-lg font-extrabold text-cargo-yellow">{option.price}</p></div>)}</div><button onClick={() => navigate("/send")} className="mt-6 flex w-full items-center justify-center gap-2 rounded-2xl bg-cargo-yellow py-4 text-sm font-bold text-ink transition hover:brightness-105">Continue with this shipment <ArrowRight className="size-4" /></button><p className="mt-3 text-center text-[11px] text-white/30">Your details will carry over to the send flow.</p></div></div></div>; }
function QuoteField({ label, icon: Icon, value, onChange, type = "text" }: { label: string; icon: typeof MapPin; value: string; onChange: (value: string) => void; type?: string }) { return <label className="block"><span className="mb-2 block text-xs font-bold uppercase tracking-[0.14em] text-white/35">{label}</span><div className="flex items-center gap-3 rounded-2xl border border-white/10 bg-ink/35 px-4"><Icon className="size-4 text-white/30" /><input type={type} value={value} onChange={(e) => onChange(e.target.value)} className="h-12 flex-1 bg-transparent text-sm font-semibold text-white outline-none" /></div></label>; }
