import { ArrowLeft, FilePenLine, Play, Trash2 } from "lucide-react";
import { useState } from "react";
import { useLocation } from "wouter";
import { Button } from "@/components/ui/button";

const key = "new-world-cargo-draft";
export default function Drafts() { const [, navigate] = useLocation(); const [draft, setDraft] = useState<any>(() => { try { const raw = localStorage.getItem(key); return raw ? JSON.parse(raw) : null; } catch { return null; } }); const remove = () => { localStorage.removeItem(key); setDraft(null); }; return <div className="mx-auto max-w-3xl text-ink"><button onClick={() => navigate("/shipments")} className="flex items-center gap-2 text-sm font-bold text-ink/55 hover:text-ink"><ArrowLeft className="size-4" /> Back to shipments</button><div className="mt-7 flex items-start gap-4"><span className="grid size-12 place-items-center rounded-2xl bg-cargo-yellow text-ink"><FilePenLine className="size-5" /></span><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-cargo-yellow">Shipment management</p><h1 className="mt-1 font-heading text-3xl font-extrabold">Shipment drafts</h1><p className="mt-2 text-sm text-ink/55">Resume a shipment request you saved before payment.</p></div></div>{draft ? <div className="mt-6 rounded-[26px] border border-ink/10 bg-white p-5"><p className="text-sm font-bold">{draft.form?.recipient || "New cargo request"}</p><p className="mt-1 text-xs text-ink/55">{draft.form?.pickup || "Pickup not selected"} · {draft.transport === "sea" ? "Sea cargo" : "Air cargo"}</p><p className="mt-4 text-xs text-ink/45">Last saved on this device. Drafts are not submitted until you complete payment.</p><div className="mt-5 flex flex-wrap gap-2"><Button onClick={() => navigate("/send?draft=latest")} className="rounded-xl bg-cargo-yellow font-bold text-ink"><Play className="mr-2 size-4" /> Resume draft</Button><Button variant="outline" onClick={remove} className="rounded-xl font-bold"><Trash2 className="mr-2 size-4" /> Delete draft</Button></div></div> : <div className="mt-6 rounded-[26px] border border-dashed border-ink/20 bg-[#f7f8fb] p-8 text-center"><p className="text-sm font-bold">No saved drafts</p><p className="mt-1 text-xs text-ink/55">Start a shipment and choose Save as Draft to keep it here.</p><Button onClick={() => navigate("/send")} className="mt-5 rounded-xl bg-cargo-yellow font-bold text-ink">Start a shipment</Button></div>}</div>; }
