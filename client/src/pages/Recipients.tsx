import { ArrowLeft, Pencil, Plus, Search, Trash2, UserRound } from "lucide-react";
import { useMemo, useState } from "react";
import { useLocation } from "wouter";
import { recipients as seedRecipients } from "@/lib/mock-data";
import type { Recipient } from "@/lib/domain";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";

const storageKey = "new-world-cargo-recipients";
const initials = (name: string) => name.split(" ").filter(Boolean).slice(0, 2).map((part) => part[0]).join("").toUpperCase() || "NW";

export default function Recipients() {
  const [, navigate] = useLocation();
  const [items, setItems] = useState<Recipient[]>(() => {
    try { const stored = localStorage.getItem(storageKey); return stored ? JSON.parse(stored) : seedRecipients; } catch { return seedRecipients; }
  });
  const [query, setQuery] = useState("");
  const [open, setOpen] = useState(false);
  const [editing, setEditing] = useState<Recipient | null>(null);
  const [form, setForm] = useState({ name: "", phone: "", location: "" });
  const saveItems = (next: Recipient[]) => { setItems(next); localStorage.setItem(storageKey, JSON.stringify(next)); };
  const shown = useMemo(() => items.filter((item) => `${item.name} ${item.location} ${item.phone}`.toLowerCase().includes(query.toLowerCase())), [items, query]);
  const openForm = (recipient?: Recipient) => { setEditing(recipient || null); setForm(recipient ? { name: recipient.name, phone: recipient.phone, location: recipient.location } : { name: "", phone: "", location: "" }); setOpen(true); };
  const submit = (event: React.FormEvent) => { event.preventDefault(); const value: Recipient = { id: editing?.id || `recipient-${Date.now()}`, ...form, initials: initials(form.name) }; saveItems(editing ? items.map((item) => item.id === editing.id ? value : item) : [value, ...items]); setOpen(false); };
  return <div className="mx-auto max-w-3xl text-ink"><button onClick={() => navigate("/settings")} className="flex items-center gap-2 text-sm font-bold text-ink/55 hover:text-ink"><ArrowLeft className="size-4" /> Back to settings</button><div className="mt-7 flex items-start gap-4"><span className="grid size-12 place-items-center rounded-2xl bg-cargo-yellow text-ink"><UserRound className="size-5" /></span><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-cargo-yellow">Settings</p><h1 className="mt-1 font-heading text-3xl font-extrabold">Saved recipients</h1><p className="mt-2 text-sm text-ink/55">Save people and businesses you send cargo to, then reuse them in a shipment.</p></div></div><div className="mt-6 flex flex-col gap-3 sm:flex-row"><label className="flex h-12 flex-1 items-center gap-2 rounded-2xl border border-ink/12 bg-white px-4"><Search className="size-4 text-ink/40" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search recipients" className="min-w-0 flex-1 bg-transparent text-sm outline-none placeholder:text-ink/35" /></label><Button onClick={() => openForm()} className="h-12 rounded-2xl bg-cargo-yellow font-bold text-ink"><Plus className="mr-2 size-4" /> Add recipient</Button></div><div className="mt-4 overflow-hidden rounded-[26px] border border-ink/10 bg-white">{shown.length ? shown.map((recipient, index) => <div key={recipient.id} className={`flex items-center gap-3 p-4 sm:p-5 ${index ? "border-t border-ink/8" : ""}`}><span className="grid size-11 shrink-0 place-items-center rounded-full bg-cargo-yellow/25 text-xs font-extrabold">{recipient.initials}</span><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold">{recipient.name}</p><p className="mt-1 truncate text-xs text-ink/50">{recipient.location} · {recipient.phone}</p></div><div className="flex gap-1"><button onClick={() => openForm(recipient)} className="grid size-9 place-items-center rounded-xl border border-ink/10 text-ink/55 hover:border-cargo-yellow"><Pencil className="size-4" /></button><button onClick={() => { if (confirm(`Delete ${recipient.name}?`)) saveItems(items.filter((item) => item.id !== recipient.id)); }} className="grid size-9 place-items-center rounded-xl border border-ink/10 text-ink/55 hover:border-cargo-yellow"><Trash2 className="size-4" /></button></div></div>) : <div className="p-8 text-center"><p className="text-sm font-bold">No recipients found</p><p className="mt-1 text-xs text-ink/50">Try another search or add a new recipient.</p></div>}</div><Dialog open={open} onOpenChange={setOpen}><DialogContent className="rounded-[26px] sm:max-w-lg"><DialogHeader><DialogTitle>{editing ? "Edit recipient" : "Add recipient"}</DialogTitle><DialogDescription>These details are available when you start a shipment.</DialogDescription></DialogHeader><form onSubmit={submit} className="grid gap-4"><label className="grid gap-2 text-xs font-bold">Name<input required autoFocus value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} className="h-11 rounded-xl border border-ink/15 px-3 text-sm" /></label><label className="grid gap-2 text-xs font-bold">Phone<input required value={form.phone} onChange={(event) => setForm({ ...form, phone: event.target.value })} className="h-11 rounded-xl border border-ink/15 px-3 text-sm" /></label><label className="grid gap-2 text-xs font-bold">City or location<input required value={form.location} onChange={(event) => setForm({ ...form, location: event.target.value })} className="h-11 rounded-xl border border-ink/15 px-3 text-sm" /></label><DialogFooter><Button type="button" variant="outline" onClick={() => setOpen(false)} className="rounded-xl font-bold">Cancel</Button><Button type="submit" className="rounded-xl bg-cargo-yellow font-bold text-ink">Save recipient</Button></DialogFooter></form></DialogContent></Dialog></div>;
}
